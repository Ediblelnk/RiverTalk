import { LineEdit, Button, ScrollView, TextEdit } from "std-widgets.slint";
import { Page } from "../../lib/page.slint";
import { SideBar } from "../../lib/side_bar.slint";

export component ProfilePage inherits Page {
    title: @tr("Profile");
    VerticalLayout {
        alignment: LayoutAlignment.start;
        spacing: 10px;
        LineEdit {
            height: 50px;
            placeholder-text: "Name";
        }

        LineEdit {
            height: 50px;
            placeholder-text: "Color";
        }
    }
}
